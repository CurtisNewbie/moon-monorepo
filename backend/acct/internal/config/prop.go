package config

// misoconfig-section: Acct Configuration
const (

	// misoconfig-prop: (`slice of objects`) built-in category
	PropAcctCategoryBuiltin = "acct.category.builtin"

	// misoconfig-prop: built-in category code
	PropAcctCategoryBuiltInCode = "acct.category.builtin.code"

	// misoconfig-prop: built-in category name
	PropAcctCategoryBuiltInName = "acct.category.builtin.name"
)
